<?php
    
    foreach ($arr as $elem) {
        echo $elem; // Элемент массива
    }

    foreach ($arr as $key => $elem) {
        echo $key . '<br>'; // Ключ массива
        echo $elem; // Элемент массива
    }
    
?>